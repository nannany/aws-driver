spring:
  datasource:
    url: jdbc:mysql:aws://my-aurora-cluster.cluster-cnmw32yasadf.ap-northeast-1.rds.amazonaws.com
    driver-class-name: software.aws.rds.jdbc.mysql.Driver
    password: ${password}
    username: admin
  flyway:
    enabled: true
